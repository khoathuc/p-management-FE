import packageInfo from '../package.json';

//TODO: remove directly address
const app = {
  version: packageInfo.version,
  name: 'Pmanage',
  logoUrl: 'http://localhost:3000/logo.svg',
};

export default app;
